git init

git config --global user.name "Yuri Menezes" 

git config --global user.email "projetohavoc@gmail.com"

ssh-keygen

abrir o arquivo ssh copiar o codigo

cat C:\Users\Yuri/.ssh/id_rsa.pub **exemplo

em seguida ir no github em configurações e ssh e cadastrar a chave

git remote add origin git@github.com:ProjetoHavoc/Django-Rest.git

conferindo se deu certo

git remote -v

pra saber se tem arquivo pra salvar no git

git status

para salvar tudo

git add .
git commit -m "first commit2"

para enviar a publicação pro github

git push





